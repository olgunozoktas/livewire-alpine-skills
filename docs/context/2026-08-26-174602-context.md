# Repository Context

## Purpose

This repository is the canonical source for two related agent skills.

- `livewire-development` covers Laravel Livewire v4 and relevant v3 differences.
- `alpinejs-development` covers Alpine.js v3 and its Livewire integration.

The Livewire skill is the stack entry point. Its `bin/stack.sh` tool locates the Alpine skill.

## Repository Structure

Each top-level skill directory is self-contained. Each directory contains a `SKILL.md`, a README, references, and validation tools.

The Livewire skill also contains test fixtures and executable recipes. The Alpine skill contains a static review tool and references.

The root README describes both skills. The root MIT license applies to the full repository.

## Runtime Contract

An agent reads each `SKILL.md` first. It reads a linked reference only when the task needs that topic.

The skills do not change an application by themselves. Their tools inspect, review, scaffold, evaluate, or validate project code.

`livewire-development/bin/scaffold.sh` is the only normal tool that writes application files. Other review and detection tools are read-only.

## External Sources

The Livewire content comes from the official Livewire `4.x` documentation. The version guide also compares the official `3.x` documentation.

The Alpine content comes from the official Alpine.js documentation. Livewire v4 bundles Alpine.js v3.

The root README records the last verified source versions and commits. Live documentation has priority when facts change.

## Validation

- Run `php livewire-security/bin/scan.php --self-test` for the security scan rules.
- Run `php livewire-security/bin/verify-facts.php <path-to-a-laravel-app>` after a Livewire upgrade. It fails when the skill states something that the installed version no longer does.
- Run `python3 livewire-development/bin/review.py --self-test` for the Livewire review rules.
- Run `python3 alpinejs-development/bin/review.py --self-test` for the Alpine review rules.
- Run `bash livewire-development/bin/eval.sh --compare` for the fixed evaluation fixtures.
- Run `bash livewire-development/bin/verify-recipes.sh` for the full recipe runtime gate.
- Run `bash livewire-development/bin/refresh.sh` to compare the snapshot with current official documentation.

The recipe gate can use Composer, PHP, and a temporary Laravel application. The refresh tool needs Git and network access.

## Source and Distribution

This repository owns the skill content. Other skill collections can vendor exact copies.

When a vendored copy changes, update this canonical repository in the same session. Record the source relation in the vendor registry.

## Hard Constraints

- Keep `alpinejs-development` separate because Alpine also works without Livewire.
- Keep `livewire-security` separate because a reader needs it at a different time. A reader needs it before a component goes on a public route, and during a security review. Most Livewire work does not need it.
- Keep `livewire-development` as the common stack entry point.
- Prefer current official documentation over the stored snapshot.
- Keep all skill instructions and user messages in ASD-STE100 Simplified Technical English.
- Do not delete files during maintenance.
- Do not start a development server during validation.

## Truth Locations

- Skill routing and mandatory rules: each `SKILL.md`.
- Detailed technical facts: each `references/` directory.
- Measured coverage and source versions: the root README and skill READMEs.
- Executable checks: each `bin/` directory and the Livewire `tests/` directory.
- Repository architecture: the newest file in `docs/context/`.

## Known Traps

- Laravel Boost can provide another skill with the same name. The project-level copy has priority.
- Livewire v4 defaults differ from v2 and v3 training data.
- A Livewire task often also needs Alpine guidance. Use `bin/stack.sh` to load both maps.
- A copied skill can drift from this repository without a source registry entry.

## Current Distribution State

The vendored copies in `olgun-skills` match all 62 skill files in this repository.

The vendor registry names this repository as the source for all three skills. Each skill description passes the standard skill validator.

## Change on 2026-08-26: the third skill

`livewire-security` is new. It holds the security half of Livewire v4.

The skill states one rule, and the other rules follow from it. A `public`
property on a Livewire component is published to the browser, and the browser
can also change it. `Drawer\BaseUtils::getPublicPropertiesDefinedOnSubclass()`
selects properties by `isPublic()` only, and
`HandleComponents::dehydrateProperties()` writes the result into the snapshot.

The skill records three facts that people state incorrectly. A model on a public
property publishes the class name and the primary key, and not every attribute.
A locked property throws
`Livewire\Features\SupportLockedProperties\CannotUpdateLockedPropertyException`,
and not an exception in `Livewire\Exceptions\`. Route middleware does not all
run again on the update endpoint, so the Spatie `permission:` middleware does not
check a permission a second time.

The skill also argues against an allow-list. An allow-list is not a boundary in
Livewire, because four paths avoid it. The skill teaches a response-level canary
sweep instead, and lists the five details that decide whether the sweep works.

`bin/scan.php` reads source text only. It needs no bootstrap, no database and no
autoloader. It applies six rules. Its self-test found three defects in the
scanner before any person trusted it. The third defect was a walk into
`packages/*/vendor`, where Livewire keeps test fixtures that contain the exact
shapes the scanner looks for. One rule reported 49 findings, and most came from
that directory. The scanner now skips `vendor` and `node_modules` at any depth.

`bin/verify-facts.php` checks the skill against the installed Livewire. The skill
names an exception namespace, the contents of a middleware list, and the shape of
a model in a snapshot. A release can change any of them, and the skill would then
give confident wrong advice. The tool checks 11 statements and fails when one no
longer holds. Two injections proved that it fails: a copy of `vendor` received
the Spatie permission middleware in the persistent list, and a copy received the
locked-property exception in `Livewire\Exceptions\`.

The `livewire-development` security section now points to this skill. The five
rules in that section stay there. The pointer covers the rest.

`references/attack-surface.md` covers the features that carry their own risk.
The most serious statement in it is verified in the Livewire source.
`#[Computed(cache: true)]` is keyed on the component name and the method name
only, in `BaseComputed.php:152`. That key holds no user and no tenant, so a
computed property that reads `auth()` serves one person's data to every person
for one hour. `#[Computed(persist: true)]` is different, because
`BaseComputed.php:145` keys it on the component instance id, and
`Factory.php:27` makes that id random for each instance.

The file also records that a browser can call any `#[On]` listener through
`__dispatch`, that the temporary upload route defaults to a throttle with no
`auth` and accepts any file type to 12 MB, that `wire:navigate` keeps the
JavaScript runtime across a page change, and that `SupportAccessingParent` is
empty in v4.4.2.

`bin/scan.php` reports the cached computed property without a key.
`bin/verify-facts.php` fails when a release changes any of these statements.
Two statements in the file are marked unverified, and each names the test that
would settle it.

The author wrote this skill from `livewire/livewire` v4.4.2 and verified each
framework statement by execution. The traps come from a security audit of a
production application.

The Livewire review tool passes 53 checks. The Alpine review tool passes 30 checks.
